
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	Если ДанныеЗаполнения = Неопределено Тогда
		
		ВалютаБюджета = Константы.ОсновнаяВалютаУчета.Получить();
		НачальнаяДатаПланирования = КонецМесяца(ТекущаяДата()) + 1;
		
		Месяц01 = Истина;
		Для НомерМесяца = 2 По 12 Цикл
			СтрокаНомерМесяца = Формат(НомерМесяца, "ЧЦ=2; ЧДЦ=0; ЧВН=");
			ЭтотОбъект["Месяц"+СтрокаНомерМесяца] = Ложь;
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры
